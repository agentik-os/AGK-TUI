# media-os Discord command pin

Pin a short owner-authored message in the dedicated `media-os` channel: mention the dedicated bot, use `/panel` for the control surface, `/clear` for a fresh session, and `/settings` for the third bounded action. Application creation, OAuth, privileged intents, and the reusable token remain owner-controlled.
